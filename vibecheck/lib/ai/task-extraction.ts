/**
 * AI task extraction stub - replace with real implementation when backend is ready
 */
export function extractTasksFromChat(
  messages: Array<{ author: string; message: string; timestamp: Date }>,
  _startDate?: Date
): Array<{ text: string; confidence?: number }> {
  return []
}
